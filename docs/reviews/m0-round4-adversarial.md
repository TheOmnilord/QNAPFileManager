Reviewed `a7a42d1`, read-only. **Significant defects remain.** All nine JavaScript tests passed; Go/Linux/ZFS runtime tests were not run.

Here, **A1–A9** mean the numbered findings in the **round-3 adversarial review**, and **S1–S5** mean the standard review’s bullet order.

1. **P2 — A7 partial; new ineffective failure throttling.** [auth_limits.go:83](C:/Dev/QNAPFileManager/internal/web/auth_limits.go:83) calls `Verify` **before** consulting the failure budget at line 88. An attacker varying tokens therefore triggers another CGI validation for every admitted request, including attempts receiving 429. The concurrency ceiling limits simultaneous work, not attempts per minute. The NAT rejection is fixed, but the replacement limiter only changes response codes. **Minimal fix:** enforce a time-based budget before uncached validation, preserving cached valid credentials and bounded admission for newcomers. Memory is bounded to 4,096 clients × 20 credential hashes; expiration and refusing additional buckets prevent unbounded growth.

2. **P2 — New: one session can occupy every authentication slot.** [session.go:166](C:/Dev/QNAPFileManager/internal/web/session.go:166) permits nine requests per session, while [auth_limits.go:105](C:/Dev/QNAPFileManager/internal/web/auth_limits.go:105) provides only eight shared execution slots. Admission precedes `lockSession` at [session.go:220](C:/Dev/QNAPFileManager/internal/web/session.go:220). One slow revalidation plus seven followers therefore occupies every slot; unrelated users, including sessions needing no QTS call, must queue. This is head-of-line blocking, not a lock-order deadlock. **Minimal fix:** keep followers within the total waiting bound but outside execution slots; only the session validator should consume expensive-work capacity.

3. **P2 — A3/S4 partial; new shutdown cleanup omission.** At [workerpool.go:409](C:/Dev/QNAPFileManager/internal/workerpool/workerpool.go:409), expiration while awaiting a startup returns immediately, before collecting and terminating already-running workers at line 417. For example, a startup stuck in `cmd.Start`, which receives no cancellation context, can exhaust the shutdown deadline. The janitor is already stopped, and subsequent `Shutdown` calls return nil because `closed` is set at [line 384](C:/Dev/QNAPFileManager/internal/workerpool/workerpool.go:384). **Minimal fix:** start cleanup of registered workers immediately and maintain one background shutdown operation covering pending startups; each caller should await that operation using its own context. Retirement accounting itself now correctly retains slots until `exited`.

4. **P2 — A5 partial: direct request paths still substitute another target.** [browse.go:32](C:/Dev/QNAPFileManager/internal/web/browse.go:32) still cleans `/dangling/../report` into `/report` before worker resolution. The same occurs with `/locked/../report`, bypassing traversal of `locked`, and `/link/../report`, potentially selecting the wrong directory. Worker entry points also clean paths, e.g. [fsops.go:581](C:/Dev/QNAPFileManager/internal/fsops/fsops.go:581). **Minimal fix:** reject noncanonical traversal components before cleaning, or preserve them through every layer for kernel-ordered resolution. The symlink-target fixes are correct: jail-boundary climbing is refused, dangling-before-`..` is retained, and [open_linux.go:127](C:/Dev/QNAPFileManager/internal/fsops/open_linux.go:127)’s `openat(fd, ".", O_PATH…)` does check directory search permission through Linux’s [pathname lookup](https://raw.githubusercontent.com/torvalds/linux/v6.8/fs/namei.c).

5. **P2 — New: an existing test can hang indefinitely during cleanup.** [workerpool_test.go:1118](C:/Dev/QNAPFileManager/internal/workerpool/workerpool_test.go:1118) creates a fake client with neither a `kill` callback nor anything closing `exited`. The newly added retirement at [client.go:380](C:/Dev/QNAPFileManager/internal/workerpool/client.go:380) can register that client before cleanup begins. Its `Shutdown(context.Background())` at line 1114 then waits forever for an impossible exit, until the test runner’s timeout. **Minimal fix:** give this fixture an idempotent termination callback that closes `exited`, and synchronize retirement before asserting cleanup.

Fully fixed round-3 findings:

- **A1 complete:** existing-image import failure aborts; imported pools require the fixture marker and vdev validation — [ci-zfs-setup.sh:130](C:/Dev/QNAPFileManager/scripts/ci-zfs-setup.sh:130), [line 161](C:/Dev/QNAPFileManager/scripts/ci-zfs-setup.sh:161).
- **A2 complete for bounded admission:** both authentication paths share the eight-active/64-waiting gate, with a per-session cap — [session.go:163](C:/Dev/QNAPFileManager/internal/web/session.go:163); fairness regression is finding 2.
- **A4/S3 complete:** absolute write deadlines cover slot acquisition, release is deferred, and terminal write failures schedule retirement — [transport.go:149](C:/Dev/QNAPFileManager/internal/wproto/transport.go:149), [line 206](C:/Dev/QNAPFileManager/internal/wproto/transport.go:206), [client.go:378](C:/Dev/QNAPFileManager/internal/workerpool/client.go:378); no additional semaphore starvation or panic-leak defect found.
- **A6/S2 complete:** parent handles use `O_PATH`, preserving access through searchable, non-readable directories — [open_linux.go:94](C:/Dev/QNAPFileManager/internal/fsops/open_linux.go:94).
- **A8 complete for the reported procfs/sysfs defect:** descriptor-based classification forces streaming — [download_linux.go:17](C:/Dev/QNAPFileManager/internal/web/download_linux.go:17), [browse.go:201](C:/Dev/QNAPFileManager/internal/web/browse.go:201).
- **A9/S5 complete:** streaming HEAD returns before any body read — [browse.go:218](C:/Dev/QNAPFileManager/internal/web/browse.go:218).
- **S1 complete:** selection preserves row identity; attributes are synchronized after selection and virtual-window reconstruction — [list.js:66](C:/Dev/QNAPFileManager/internal/web/static/js/list.js:66), [line 120](C:/Dev/QNAPFileManager/internal/web/static/js/list.js:120).

Round-3’s carried-forward closures also remain complete:

- Cancelled-startup restart charging — [workerpool.go:774](C:/Dev/QNAPFileManager/internal/workerpool/workerpool.go:774).
- Domain-cookie decoding and CGI escaping — [cred.go:104](C:/Dev/QNAPFileManager/internal/qtsauth/cred.go:104), [client.go:225](C:/Dev/QNAPFileManager/internal/qtsauth/client.go:225).
- Session eviction isolation — [session.go:110](C:/Dev/QNAPFileManager/internal/web/session.go:110).
- Locked `abandoned` inspection — [client.go:244](C:/Dev/QNAPFileManager/internal/workerpool/client.go:244).
- Linux final-component no-follow and blocking-mode restoration — [open_linux.go:65](C:/Dev/QNAPFileManager/internal/fsops/open_linux.go:65), [fsops.go:630](C:/Dev/QNAPFileManager/internal/fsops/fsops.go:630).
- Obsolete session continuations discarded — [viewer.js:28](C:/Dev/QNAPFileManager/internal/web/static/js/viewer.js:28).
- ZFS packaging gate — [build.yml:127](C:/Dev/QNAPFileManager/.github/workflows/build.yml:127).
- Worker-error reconstruction and HTTP 503 mapping — [client.go:566](C:/Dev/QNAPFileManager/internal/workerpool/client.go:566), [jsonhttp.go:32](C:/Dev/QNAPFileManager/internal/web/jsonhttp.go:32).

**Residual risks:** `stopRetiredWait` bounds each request-side wait to ten seconds; `awaitExit` intentionally persists, and shutdown without a deadline can wait forever on an unkillable worker. Resolution/open TOCTOU remains. The pseudo-filesystem list is not exhaustive; the non-Linux stub performs no classification. Blocking pseudo-file reads may resist cancellation. ZFS mount checks still establish presence rather than dataset identity. QTS proxy/authentication behavior and QNAP kernel/ZFS semantics still require hardware verification.